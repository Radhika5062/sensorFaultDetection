# This means that we can access this application from any system. If we give local host then we will
# only be able to access it via local host which means our own system.
APP_HOST = "0.0.0.0"
APP_PORT = 8080